<%= render :partial => 'admin/shared/tariff_manager_menu' %>

<h1>Editovat operátora</h1>
<%= error_messages_for :operator %>

<% form_for(:operator, :url => object_url, :html => { :method => :put }) do |f| -%>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("update") %> <%= t("or") %> <%= link_to t("cancel"), admin_operators_path %>
  </p>
<% end %>